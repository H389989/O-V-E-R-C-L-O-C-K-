-- StarterPlayer > StarterPlayerScripts > CoolAdminMenu (LocalScript)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local myHumanoid = nil

player.CharacterAdded:Connect(function(char)
    myHumanoid = char:WaitForChild("Humanoid")
end)
if player.Character then
    myHumanoid = player.Character:FindFirstChildOfClass("Humanoid")
end

local noclipEnabled = false
local immortalityEnabled = false

-- GUI Setup
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "CoolAdminMenu"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

-- Taller frame for new button
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 280, 0, 520)
mainFrame.Position = UDim2.new(0.5, -140, 0.5, -260)
mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Visible = false
mainFrame.Parent = screenGui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 50)
title.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
title.Text = "⚡ COOL ADMIN MENU ⚡"
title.TextColor3 = Color3.new(1, 1, 1)
title.TextScaled = true
title.Font = Enum.Font.GothamBold
title.Parent = mainFrame

local noclipBtn = Instance.new("TextButton")
noclipBtn.Size = UDim2.new(0.9, 0, 0, 45)
noclipBtn.Position = UDim2.new(0.05, 0, 0.12, 0)
noclipBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
noclipBtn.Text = "Noclip: OFF"
noclipBtn.TextColor3 = Color3.new(1, 1, 1)
noclipBtn.TextScaled = true
noclipBtn.Font = Enum.Font.GothamSemibold
noclipBtn.Parent = mainFrame

local immortalityBtn = Instance.new("TextButton")
immortalityBtn.Size = UDim2.new(0.9, 0, 0, 45)
immortalityBtn.Position = UDim2.new(0.05, 0, 0.23, 0)
immortalityBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
immortalityBtn.Text = "Immortality: OFF"
immortalityBtn.TextColor3 = Color3.new(1, 1, 1)
immortalityBtn.TextScaled = true
immortalityBtn.Font = Enum.Font.GothamSemibold
immortalityBtn.Parent = mainFrame

local killAllBtn = Instance.new("TextButton")
killAllBtn.Size = UDim2.new(0.9, 0, 0, 45)
killAllBtn.Position = UDim2.new(0.05, 0, 0.34, 0)
killAllBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
killAllBtn.Text = "💀 KILL ALL OTHERS"
killAllBtn.TextColor3 = Color3.new(1, 1, 1)
killAllBtn.TextScaled = true
killAllBtn.Font = Enum.Font.GothamBold
killAllBtn.Parent = mainFrame

local getItemsBtn = Instance.new("TextButton")  -- NEW BUTTON
getItemsBtn.Size = UDim2.new(0.9, 0, 0, 45)
getItemsBtn.Position = UDim2.new(0.05, 0, 0.45, 0)
getItemsBtn.BackgroundColor3 = Color3.fromRGB(255, 140, 0)
getItemsBtn.Text = "🎁 GET ALL ITEMS"
getItemsBtn.TextColor3 = Color3.new(1, 1, 1)
getItemsBtn.TextScaled = true
getItemsBtn.Font = Enum.Font.GothamBold
getItemsBtn.Parent = mainFrame

-- Teleport Section
local tpLabel = Instance.new("TextLabel")
tpLabel.Size = UDim2.new(0.9, 0, 0, 25)
tpLabel.Position = UDim2.new(0.05, 0, 0.58, 0)
tpLabel.BackgroundTransparency = 1
tpLabel.Text = "Teleport Section"
tpLabel.TextColor3 = Color3.new(1, 1, 1)
tpLabel.TextScaled = true
tpLabel.Font = Enum.Font.GothamBold
tpLabel.Parent = mainFrame

local usernameBox = Instance.new("TextBox")
usernameBox.Size = UDim2.new(0.9, 0, 0, 40)
usernameBox.Position = UDim2.new(0.05, 0, 0.63, 0)
usernameBox.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
usernameBox.PlaceholderText = "Enter username..."
usernameBox.Text = ""
usernameBox.TextColor3 = Color3.new(1, 1, 1)
usernameBox.TextScaled = true
usernameBox.Font = Enum.Font.Gotham
usernameBox.Parent = mainFrame

local tpToMeBtn = Instance.new("TextButton")
tpToMeBtn.Size = UDim2.new(0.9, 0, 0, 45)
tpToMeBtn.Position = UDim2.new(0.05, 0, 0.74, 0)
tpToMeBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
tpToMeBtn.Text = "⬇️ BRING PLAYER TO ME"
tpToMeBtn.TextColor3 = Color3.new(1, 1, 1)
tpToMeBtn.TextScaled = true
tpToMeBtn.Font = Enum.Font.GothamBold
tpToMeBtn.Parent = mainFrame

local meToThemBtn = Instance.new("TextButton")
meToThemBtn.Size = UDim2.new(0.9, 0, 0, 45)
meToThemBtn.Position = UDim2.new(0.05, 0, 0.86, 0)
meToThemBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
meToThemBtn.Text = "🚀 TELEPORT TO PLAYER"
meToThemBtn.TextColor3 = Color3.new(1, 1, 1)
meToThemBtn.TextScaled = true
meToThemBtn.Font = Enum.Font.GothamBold
meToThemBtn.Parent = mainFrame

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -35, 0, 10)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
closeBtn.Text = "✕"
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.TextScaled = true
closeBtn.Font = Enum.Font.GothamBold
closeBtn.Parent = mainFrame

-- Mobile Toggle
local mobileToggle = Instance.new("TextButton")
mobileToggle.Size = UDim2.new(0, 60, 0, 60)
mobileToggle.Position = UDim2.new(0, 20, 0.5, -30)
mobileToggle.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
mobileToggle.Text = "⚡"
mobileToggle.TextColor3 = Color3.new(1, 1, 1)
mobileToggle.TextScaled = true
mobileToggle.Font = Enum.Font.GothamBold
mobileToggle.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1, 0)
corner.Parent = mobileToggle

-- ==================== FUNCTIONS ====================

local function toggleMenu()
    mainFrame.Visible = not mainFrame.Visible
end

-- Noclip
local noclipConnection
noclipBtn.MouseButton1Click:Connect(function()
    noclipEnabled = not noclipEnabled
    noclipBtn.Text = "Noclip: " .. (noclipEnabled and "ON" or "OFF")
    noclipBtn.BackgroundColor3 = noclipEnabled and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(40, 40, 50)
    
    if noclipEnabled then
        noclipConnection = RunService.Stepped:Connect(function()
            if player.Character then
                for _, part in pairs(player.Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
            end
        end)
    else
        if noclipConnection then noclipConnection:Disconnect() end
    end
end)

-- Immortality
immortalityBtn.MouseButton1Click:Connect(function()
    immortalityEnabled = not immortalityEnabled
    immortalityBtn.Text = "Immortality: " .. (immortalityEnabled and "ON" or "OFF")
    immortalityBtn.BackgroundColor3 = immortalityEnabled and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(40, 40, 50)
    
    if immortalityEnabled then
        spawn(function()
            while immortalityEnabled and player.Character do
                local hum = player.Character:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum.MaxHealth = math.huge
                    hum.Health = math.huge
                end
                task.wait(0.1)
            end
        end)
    end
end)

-- Kill All Others (Game-wide)
killAllBtn.MouseButton1Click:Connect(function()
    local killed = 0
    for _, humanoid in pairs(game:GetDescendants()) do
        if humanoid:IsA("Humanoid") then
            if humanoid == myHumanoid or (humanoid.Parent and humanoid.Parent == player.Character) then
                continue
            end
            humanoid.Health = 0
            humanoid:TakeDamage(999999)
            
            task.spawn(function()
                for i = 1, 4 do
                    if humanoid and humanoid.Parent then
                        humanoid.Health = 0
                        local char = humanoid.Parent
                        if char:FindFirstChild("Head") then char.Head:Destroy() end
                        if char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart:Destroy() end
                    end
                    task.wait(0.08)
                end
            end)
            killed += 1
        end
    end
    print("💀 Kill All executed - Targeted " .. killed .. " humanoids")
end)

-- 🎁 NEW: Get All Items
getItemsBtn.MouseButton1Click:Connect(function()
    local backpack = player:FindFirstChild("Backpack")
    if not backpack then return end
    
    local collected = 0
    
    for _, item in pairs(game:GetDescendants()) do
        if item:IsA("Tool") or item:IsA("HopperBin") then
            -- Skip items already in our backpack or character
            if item.Parent == backpack or (player.Character and item.Parent == player.Character) then
                continue
            end
            
            local clone = item:Clone()
            clone.Parent = backpack
            collected += 1
        end
    end
    
    print("🎁 Collected " .. collected .. " items into your backpack!")
end)

-- Bring Player TO Me
tpToMeBtn.MouseButton1Click:Connect(function()
    local targetName = usernameBox.Text:match("^%s*(.-)%s*$")
    if targetName == "" then
        print("❌ Please enter a username")
        return
    end
    
    local targetPlayer = Players:FindFirstChild(targetName)
    if not targetPlayer then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr.Name:lower():find(targetName:lower()) then
                targetPlayer = plr
                break
            end
        end
    end
    
    if targetPlayer and targetPlayer ~= player and targetPlayer.Character then
        local myRoot = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        if myRoot and targetRoot then
            targetRoot.CFrame = myRoot.CFrame + Vector3.new(3, 2, 0)
            print("⬇️ Brought " .. targetPlayer.Name .. " to you")
        end
    else
        print("❌ Player not found")
    end
end)

-- Teleport TO Player
meToThemBtn.MouseButton1Click:Connect(function()
    local targetName = usernameBox.Text:match("^%s*(.-)%s*$")
    if targetName == "" then
        print("❌ Please enter a username")
        return
    end
    
    local targetPlayer = Players:FindFirstChild(targetName)
    if not targetPlayer then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr.Name:lower():find(targetName:lower()) then
                targetPlayer = plr
                break
            end
        end
    end
    
    if targetPlayer and targetPlayer.Character then
        local root = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        local myRoot = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if root and myRoot then
            myRoot.CFrame = root.CFrame + Vector3.new(0, 3, 0)
            print("🚀 Teleported to " .. targetPlayer.Name)
        end
    else
        print("❌ Player not found")
    end
end)

-- Close
closeBtn.MouseButton1Click:Connect(function()
    mainFrame.Visible = false
end)

-- Mobile Toggle
mobileToggle.MouseButton1Click:Connect(toggleMenu)

-- PC Toggle
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.Insert then
        toggleMenu()
    end
end)

print("✅ Cool Admin Menu loaded with Get All Items!")
print("Mobile: Tap ⚡ | PC: Press Insert")
